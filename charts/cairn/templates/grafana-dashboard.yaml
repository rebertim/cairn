{{- if index .Values "victoria-metrics-k8s-stack" "grafana" "enabled" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cairn.fullname" . }}-dashboard
  labels:
    grafana_dashboard: "1"
  {{- include "cairn.labels" . | nindent 4 }}
data:
  cairn-dashboard.json: |-
{{ .Files.Get "dashboards/cairn-dashboard.json" | indent 4 }}
{{- end }}
