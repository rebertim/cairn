controllerManager:
  manager:
    args:
    - --metrics-bind-address=:8080
    - --metrics-secure=false
    - --leader-elect
    - --health-probe-bind-address=:8081
    - --webhook-cert-path=/tmp/k8s-webhook-server/serving-certs
    - --agent-image=ghcr.io/rebertim/cairn-agent:latest
    containerSecurityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop:
        - ALL
      readOnlyRootFilesystem: true
    env:
      # When kube-prometheus-stack.enabled is true this is auto-computed from the bundled stack.
      # When kube-prometheus-stack.enabled is false, set this to your Prometheus URL.
      # ServiceMonitor and PodMonitor are not deployed in BYOP mode — scraping setup
      # is the customer's responsibility.
      prometheusUrl: ""
      # How often the policy controller polls workloads and recomputes recommendations
      reconcileInterval: "2m"
      # cairn-agent JAR image injected into detected Java pods
      agentImage: "ghcr.io/rebertim/cairn-agent:latest"
      # Multi-cluster Prometheus query scoping. Leave both empty to disable.
      # clusterName: the label value for this cluster (e.g. "e1-k8s-pfnet-lab-t")
      # clusterLabelName: the label key in your Prometheus (e.g. "cluster" or "kubernetes_cluster_name")
      clusterName: ""
      clusterLabelName: ""
    image:
      repository: ghcr.io/rebertim/cairn
      tag: ""
    imagePullPolicy: IfNotPresent
    resources:
      limits:
        cpu: 500m
        memory: 128Mi
      requests:
        cpu: 10m
        memory: 64Mi
  podAnnotations: {}
  nodeSelector: {}
  podSecurityContext:
    runAsNonRoot: true
    seccompProfile:
      type: RuntimeDefault
  replicas: 1
  tolerations: []
  topologySpreadConstraints: []
kubernetesClusterDomain: cluster.local
metricsService:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  type: ClusterIP
serviceAccount:
  annotations: {}
  automount: true
  create: true
  name: ""
webhookService:
  ports:
  - port: 443
    protocol: TCP
    targetPort: 9443
  type: ClusterIP

# ---------------------------------------------------------------------------
# Bundled monitoring stack (kube-prometheus-stack)
# ---------------------------------------------------------------------------
# Enabled by default so a fresh install gets Prometheus + Grafana out of the
# box. Set enabled: false and provide controllerManager.manager.env.prometheusUrl
# to use an existing Prometheus instance instead.
kube-prometheus-stack:
  enabled: true
  alertmanager:
    enabled: false
  prometheus:
    prometheusSpec:
      # Use the default release-label selector so kubelet/cAdvisor monitors are included.
      # Our ServiceMonitor and PodMonitor carry release: <release-name> to match.
      podMonitorNamespaceSelector: {}
  grafana:
    enabled: true
    sidecar:
      dashboards:
        enabled: true
        label: grafana_dashboard
        searchNamespace: ALL
