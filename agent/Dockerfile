# The agent image is used exclusively as an ImageVolume — it just needs to
# contain agent.jar at /agent.jar. scratch gives the smallest possible image.
FROM scratch
COPY build/libs/agent.jar /agent.jar
